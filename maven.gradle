apply plugin: 'maven'

uploadArchives {
    doFirst {
        delete fileTree("$buildDir/maven-repo")
    }
    repositories {
        mavenDeployer {
            repository(url: "file://./$buildDir/maven-repo") {
            }
        }
    }
}
